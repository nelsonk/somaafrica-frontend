import { TrackSourceDirective } from './track-source.directive';

describe('TrackSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackSourceDirective();
    expect(directive).toBeTruthy();
  });
});
